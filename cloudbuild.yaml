steps:
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: ['-c', 'docker pull gcr.io/$PROJECT_ID/test:latest || exit 0']
- name: 'gcr.io/cloud-builders/docker'
  args: [
            'build',
            '--tag', 'gcr.io/$PROJECT_ID/test:latest',
            '--cache-from', 'gcr.io/$PROJECT_ID/test:latest',
            '.'
        ]
images: ['gcr.io/$PROJECT_ID/test:latest']
